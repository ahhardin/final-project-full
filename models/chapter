const mongoose = require('mongoose')
const Schema = mongoose.Schema

const ProductSchema = new Schema({
  category: String,
  name: String,
  subcategories: [ReviewSchema]
})

module.exports = mongoose.model('Product', ProductSchema)